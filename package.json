{
  "name": "html-to-figma-plugin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:plugin": "node scripts/build-plugin.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "vite": "^5.4.2"
  }
}
